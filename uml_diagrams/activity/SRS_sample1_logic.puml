@startuml
'style options
skinparam activity {
  StartColor #green
  EndColor #red
  BackgroundColor #F0F0F0
  BorderColor Black
}

start

:Home Screen Loaded/Refreshed (Trigger Range Calculation);

:Retrieve Current SoC, Recent Energy Consumption Trends, Ambient Temperature from Vehicle ECU;

if (Ambient Temperature Data from Vehicle ECU Available?) then (yes)
  :Proceed to Step 5;
else (no)
  :Call External Temperature Service;
endif

if (External Temperature Data Retrieved Successfully?) then (yes)
  :Proceed to Step 6;
else (no)
  :Proceed to Step 10;
endif

:Range Calculation (SoC, Energy Consumption, Temperature, Vehicle Efficiency);

:Unit Conversion (to User's Preferred Unit);

:Data Validation (Check if Range is Within Reasonable Limits);

if (Calculated Range Valid?) then (yes)
  :Proceed to Step 12;
else (no)
  :Proceed to Step 10;
endif

:Display Default/Backup Range;
:Error Logging;

:Display Estimated Vehicle Range on Home Screen;

stop
@enduml